# Change Card: S6-B Invalid-Model 404 Recovery Release Audit

## Identity

- Signals: `G-05 invalid_model_404_recovery`, `R-03 response_headers_error_bodies` (manager/lifecycle subset)
- Branch: `feat/s6b-404-recovery`
- Baseline commit: `571fba462156021785c23a755f748b7c4c3cd1aa`
- Date: 2026-09-08
- Observation boundaries: CPA source/unit tests, local eight-stage capture, and existing real B-GAP wire evidence

## Current evidence and confounders

The valid B-GAP run `artifacts/CPA-ClaudeCode-risk-harness-20260823/runs/gap-only-20260828-060618/` contains a real invalid-model HTTP 404 followed by same-session recovery. The 2026-09-08 rerun stopped at provider-side Messages HTTP 401 before the invalid-model override could be reached; it is recorded as `BLOCKED_BY_MESSAGES_401` and is not used as a behavior failure.

The A/B hosts, egress, account/cohort, model and workspace are not identical. No provider-receiver observation is claimed.

## Scope

This card closes the release-grade audit of existing 404 classification and recovery behavior. It does not add a new retry policy, alter TLS/HTTP/body fields, or modify production source/configuration.

Allowed evidence:

- Existing valid B 404/recovery capture.
- Focused manager tests for generic route 404, explicit model 404, and request-scoped 404.
- Focused `cc_prev_req` tests proving failed requests do not advance continuity.

Prohibited claims:

- A 401/403/429 is not a substitute for a 404.
- This card does not claim Anthropic receiver-side raw H1 or strict A/B equivalence.
- No account-lifetime or credential-health inference is made.

## Acceptance and falsifiers

Acceptance requires all of the following:

1. Explicit model 404 cools only the invalid model state.
2. Generic route 404 stays availability-neutral and does not block fallback.
3. Request-scoped 404 does not cool the credential or unrelated model state.
4. Failed requests do not advance `cc_prev_req`; recovery references the last successful response.
5. The blocked 2026-09-08 rerun remains excluded from behavioral attribution.

Falsifiers are cross-credential cooling, failed-chain advancement, or a new source/configuration diff outside this card.

## Verification and rollback

- Artifact directory: `artifacts/CPA-CLAUDECODE-S6B-404-RELEASE-20260908/`.
- Baseline/modified focused tests and exact outputs are recorded in `VERIFICATION.txt`.
- `ROLLBACK.sh` was executed against an independent copy and returned `RESTORED=1; HASH_MATCH=True`.
- Production `CLIProxyAPI` remained `Running`; no production file was changed.

## Decision

`S6-B behavior: CAPTURED_B` using the existing valid B-GAP evidence.

`S6-B release gate: CLOSED_BY_EVIDENCE_AND_TESTS`.

The overall release remains `NOT STRICTLY EQUIVALENT` because diagnostics/prior/prompt parity, TLS lifecycle, strict same-environment causality, and provider-side boundaries remain separate signals.
